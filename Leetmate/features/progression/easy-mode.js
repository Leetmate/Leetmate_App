// Easy Mode toggle logic for Settings screen.
(function () {
  "use strict";

  function hasFirebase() {
    return typeof firebase !== "undefined" && firebase.auth && firebase.firestore;
  }

  
})();
